#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
int A[41];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, s; cin >> n >> s;
    for (int i = 0; i < n; i++) {

    }

    return 0;
}
